package provider

import (
	"context"
	"math/big"
)

func (client *ProviderClient) Publish(ctx context.Context, id *big.Int) {

	// publish

	// <- notify
	// wait for deploying

	// run faas

	// fulfill
}
